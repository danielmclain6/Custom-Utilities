<UserControl x:Class="CustomUtils.Utilities.Reminders.RemindersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Bg}">
    <Grid>
        <!-- LIST PANE -->
        <Grid x:Name="ListPane">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="32,28,32,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Reminders" FontSize="22" FontWeight="SemiBold"/>
                    <TextBlock Text="Schedule a Windows notification for any date and time."
                               Foreground="{DynamicResource MutedFg}" FontSize="12" Margin="0,4,0,0"/>
                </StackPanel>
                <Button Grid.Column="1"
                        x:Name="NewButton"
                        Style="{StaticResource PrimaryButton}"
                        Content="+ New reminder"
                        Click="NewButton_Click"/>
            </Grid>

            <!-- Card list -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="32,0,32,24">
                <StackPanel>
                    <TextBlock x:Name="EmptyHint"
                               Text="No reminders yet. Click + New reminder to add one."
                               Foreground="{DynamicResource MutedFg}"
                               Margin="4,32,0,0"
                               Visibility="Collapsed"/>

                    <TextBlock Text="Upcoming" FontSize="13" FontWeight="SemiBold"
                               Foreground="{DynamicResource MutedFg}" Margin="4,0,0,8"/>
                    <ItemsControl x:Name="UpcomingList"/>

                    <TextBlock Text="Past" FontSize="13" FontWeight="SemiBold"
                               Foreground="{DynamicResource MutedFg}" Margin="4,18,0,8"/>
                    <ItemsControl x:Name="PastList"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!-- EDIT PANE -->
        <ContentControl x:Name="EditPaneHost" Visibility="Collapsed"/>
    </Grid>
</UserControl>
